This keystore is used for testing purposes only.

Keystore pass is: devjks
Key pass is: devjks

To list the content of the keystore use this command:
keytool -list -v -keystore ./dev.jks -storepass devjks -keypass devjks

The keystore contains the following keys/certs:
vgr_ca - Trusted self signed VGR CA Root Cert (not real)
vgr-pdl - Private key and signed certificate by vgr_ca

Please have a look in the test directory for usage to sing a XML-structure

The certs are signed until 2023-10-23 (ten years).

Written by Magnus Andersson (magnus.andersson@mollyware.se)